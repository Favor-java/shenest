import{useEffect,useState}from'react';import{Link}from'react-router-dom';import{api}from'../api/api.js';
export default function LandlordDashboard(){const loggedIn=Boolean(localStorage.getItem('shenest_token'));const[properties,setProperties]=useState([]);const[loading,setLoading]=useState(loggedIn);useEffect(()=>{if(loggedIn)api('/properties/mine').then(setProperties).finally(()=>setLoading(false))},[loggedIn]);if(!loggedIn)return <main className="empty-page"><div><h1>Landlord dashboard</h1><p>Log in to manage your listings.</p><Link className="button" to="/login">Log in</Link></div></main>;return <main className="page section"><div className="dashboard-heading"><div><p className="eyebrow">Landlord dashboard</p><h1>Your properties</h1><p>See your listings and whether SheNest has verified them.</p></div><Link className="button" to="/properties/new">+ Add property</Link></div>{loading&&<p>Loading...</p>}<div className="dashboard-list">{properties.map(p=><article className="dashboard-row" key={p.id}><img src={p.image||'https://images.unsplash.com/photo-1600607687920-4e2a09cf159d?auto=format&fit=crop&w=300&q=70'} alt=""/><div><h3>{p.title}</h3><p>{p.location} · ₦{p.price.toLocaleString()}/yr</p></div><span className={p.verified?'status verified-status':'status pending-status'}>{p.verified?'Verified':'Pending verification'}</span><Link to={`/properties/${p.id}`}>View →</Link></article>)}</div></main>}
